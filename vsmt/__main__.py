import ping
import sensor
import cli

if __name__ == "__main__":
    cli.cli()
    # execute only if run as a script
    #print(ping.ping('wp.pl',1,1))
    #sensor.sensor_ping('wp.pl',10,0.2,5)        